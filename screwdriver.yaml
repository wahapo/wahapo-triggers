jobs:
  main:
    requires: [ ~pr, ~commit ]
    image: node:8
    steps:
      - ls: ls
  main2:
    requires: [ ~main ]
    image: node:8
    steps:
      - ls: ls
  from-parent-trigger:
    requires: [ ~sd@1:main-2 ]
    image: node:8
    steps:
      - ls: ls
